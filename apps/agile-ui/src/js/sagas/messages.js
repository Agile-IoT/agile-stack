// import { delay, takeEvery } from 'redux-saga'
// import * as types from '../constants/ActionTypes'
// import { call, put, fork } from 'redux-saga/effects'
//
// export function* messageRemove(action) {
//   // keep error in state for 5s
//   var actionType = action.type.substring(0, action.type.lastIndexOf('_'));
//   yield call(delay, 4000)
//   yield put({ type: `${actionType}_REMOVE`})
// }
//
// export function* rootMessagesSaga() {
//   yield takeEvery([types.ERRORS_ADD, types.CONFIRMATIONS_ADD], messageRemove)
// }

// not in use but should be 
